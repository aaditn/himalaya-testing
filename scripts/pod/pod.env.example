# Copy to scripts/pod/pod.env and fill in. pod.env is gitignored; this file is
# tracked, so it must never hold real host details.
POD_HOST=0.0.0.0
POD_PORT=22
POD_USER=root
POD_KEY=$HOME/.ssh/key_file
